---

## slug: /utc-date

# UTC_DATE

## 宣言

```sql
UTC_DATE()
```

## 説明

現在のUTC日付を`%Y-%m-%d`形式で返します。

## 例

```sql
SELECT UTC_DATE(), UTC_DATE() + 0, UTC_DATE() + 6;
+------------+----------------+----------------+
| UTC_DATE() | UTC_DATE() + 0 | UTC_DATE() + 6 |
+------------+----------------+----------------+
| 2025-12-18 |       20251218 |       20251224 |
+------------+----------------+----------------+
1 row in set (0.001 sec)
```