---
slug: /charset

---

# CHARSET

## 宣言

```sql
CHARSET(str)
```

## 説明

文字列 `str` が使用する文字セットを返します。

## 例

```sql
SELECT CHARSET("oceanbaase");
+-----------------------+
| CHARSET("oceanbaase") |
+-----------------------+
| utf8mb4               |
+-----------------------+
1 row in set (0.001 sec)

SELECT CHARSET(CONVERT("oceanbase" USING binary));
+--------------------------------------------+
| CHARSET(CONVERT("oceanbase" USING binary)) |
+--------------------------------------------+
| binary                                     |
+--------------------------------------------+
1 row in set (0.001 sec)
```