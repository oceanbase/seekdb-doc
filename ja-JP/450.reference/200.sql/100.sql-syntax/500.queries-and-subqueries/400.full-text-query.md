---

slug: /full-text-query

---

# フルテキスト検索

フルテキスト検索とは、テキストデータ内で全文を対象に検索または調査を行う操作です。特定のキーワード、フレーズ、またはテキスト表現を含むテキスト内容を見つけるために使用されます。フルテキスト検索は、テキスト全体をより包括的に検索し、検索条件に一致する結果を返します。

## 構文

以下の構文を使用して全文インデックスクエリを実行すると、指定された列とキーワードまたはフレーズに基づいて全文検索を実行できます。特定の検索修飾子を使用して検索パターン（つまり、検索条件やルール）を調整することもできます。

```sql
MATCH (column_name [, column_name ...]) AGAINST (expr [search_modifier])

search_modifier:
    IN NATURAL LANGUAGE MODE
    | IN BOOLEAN MODE
```

**関連パラメータの説明は以下の通りです：**

* `column_name`：全文検索を実行する列を指定します。複数の列を指定する場合は、カンマで区切ります。

* `expr`：検索するキーワードまたはフレーズを指定します。

* `search_modifier`：オプションです。検索パターンを指定するために使用します。値は以下のとおりです：

  * `IN NATURAL LANGUAGE MODE`：デフォルト値です。自然言語検索モードを使用して検索することを指定します。

  * `IN BOOLEAN MODE`：ブールモードを使用して検索することを指定します。現在のバージョンでは、最も一般的に使用される3つのブール演算子と、演算子のネストがサポートされています。詳細は以下のとおりです：

    * `+`：`AND`、すなわち共通部分を表します。

    * `-`：否定、すなわち差集合を表します。

    * 無操作記号：単独で使用すると `OR`、すなわち和集合を表します。例えば、`A B` は `A OR B` を表します。和記号との混用は、存在する文の関連性を高めますが、`OR` の意味を失います。例えば、`+A B` は、A が必要であり、文中の A と B の関連性を計算することを意味します。

    * `()`：演算子のネストを表します。外側に記号がない場合、`OR` の意味を持ちます。例えば、`+A (ネストされた句)` は、A が必要、またはネストされた句が必要であることを表します。

    **例：**

    1. 出力される文には必ず computer が含まれている必要があります。

       ```sql
       SELECT * FROM my_table WHERE MATCH (doc) AGAINST ("+computer" IN BOOLEAN MODE);
       ```

    2. 出力される文には必ず computer が含まれている必要があり、weather は含まれていてはなりません。

       ```sql
       SELECT * FROM my_table WHERE MATCH (doc) AGAINST ("+computer -weather" IN BOOLEAN MODE);
       ```

    3. 出力される文には必ず computer が含まれている必要があり、oceanbase が含まれている方がより一致します。

       ```sql
       SELECT * FROM my_table WHERE MATCH (doc) AGAINST ("+computer oceanbase" IN BOOLEAN MODE);
       ```

`MATCH AGAINST` 式の詳細については、[MATCH AGAINST](../400.functions/1000.other-functions/2250.match-against.md)を参照してください。

## ベクトル化クエリ

全文インデックスを含むクエリでは、ベクトル化と非ベクトル化を選択できます。`/*+ opt_param('rowsets_enabled', '[true | false]')*/` ヒントを使用して、ベクトル化を有効または無効にすることができます。

:::tip

ヒントが指定されていない場合、ベクトル化を有効にするかどうかは、ベクトル化エンジンのシステムパラメータ設定によって影響を受け、デフォルトでは有効になっています。

:::

**例：**

* ベクトル化クエリを有効にします。

  ```sql
  SELECT /*+ opt_param('rowsets_enabled', 'true') */ title, body
  FROM articles
  WHERE MATCH(title, body) AGAINST('tutorial');
  ```

* ベクトル化クエリを無効にします。

  ```sql
  SELECT /*+ opt_param('rowsets_enabled', 'false') */ title, body
  FROM articles
  WHERE MATCH(title, body) AGAINST('tutorial');
  ```

## 例

1. テーブル `tbl1` を作成し、全文インデックス `full_idx1_tbl1` も同時に作成します。

   ```sql
   CREATE TABLE tbl1(col1 INT PRIMARY KEY, col2 VARCHAR(100), col3 TEXT, FULLTEXT INDEX full_idx1_tbl1(col2, col3));
   Query OK, 0 rows affected (0.101 sec)
   ```

2. テーブル `tbl1` にテストデータを追加します。

   ```sql
   INSERT INTO tbl1 (col1, col2, col3) VALUES (1, 'Hello World', 'This is a test'),
       (2, 'seekdb', 'seekdb is an AI-native search database. It unifies relational, vector, text, JSON and GIS in a single engine, enabling hybrid search and in-database AI workflows.'),
       (3, 'Database Management', 'Learn about SQL and database administration'),
       (4, 'Full Text Searching', 'Master the art of full text searching');
   ```

   実行結果は次のとおりです：

   ```shell
   Query OK, 4 rows affected (0.003 sec)
   Records: 4  Duplicates: 0  Warnings: 0
   ```

3. `MATCH` 句で、テーブル `tbl1` の `col2` 列と `col3` 列を検索する対象として指定し、検索キーワードは 'seekdb' とします。また、検索修飾子 `IN NATURAL LANGUAGE MODE` を使用し、自然言語検索モードで検索を実行することを示します。

```sql
SELECT * FROM tbl1
WHERE MATCH (col2, col3) AGAINST ('seekdb' IN NATURAL LANGUAGE MODE);
```

実行結果は次のとおりです：

```shell
+------+--------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| col1 | col2   | col3                                                                                                                                                                                                 |
+------+--------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
|    2 | seekdb | seekdb is an AI-native search database. It unifies relational, vector, text, JSON and GIS in a single engine, enabling hybrid search and in-database AI workflows. |
+------+--------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
1 row in set (0.002 sec)
```

## 関連ドキュメント

* [インデックスの作成](../../../../200.develop/200.design-database-schema/40.create-index-in-develop.md)
* [全文インデックス](../../../../200.develop/200.design-database-schema/35.multi-model/300.char-and-text/300.full-text-index.md)