---

## slug: /set-default-role

# SET DEFAULT ROLE

## 説明

このステートメントは、ユーザーがログインする際にデフォルトで有効にするロールを設定します。

## 使用制限と注意事項

現在のユーザーが指定しようとしているロールを持っていなければ、この操作を実行できません。

## 権限要件

:::tip
ユーザーが自分自身に対して、既に付与されているロールをデフォルトで有効に設定する場合、特別な権限は必要ありません。
:::

ロールを有効にするには、現在のユーザーが `CREATE USER` 権限を持っている必要があります。seekdb権限の詳細については、[seekdbの権限分類](../../../../400.guides/500.manage/300.security/300.access-control/200.user-and-permission/100.permission-classification.md)を参照してください。

## 構文

```sql
SET DEFAULT ROLE
    {NONE | ALL | role_name [, role_name ...]}
    TO user_name [, user_name ...];
```

## パラメータ説明

|       **パラメータ**      | **説明** |
|---------------------|----------|
| NONE                | ユーザーが付与されたすべてのロールを無効にすることを指定します。|
| ALL                 | ユーザーのすべてのロールを有効にすることを指定します。|
| role_name           | ロール名を指定します。複数のロールを有効にする場合は、ロール名を半角カンマ（`,`）で区切ります。|
| user_name           | ユーザー名を指定します。複数のユーザーのロールを有効にする場合は、ユーザー名を半角カンマ（`,`）で区切ります。|

## 例

ユーザー `user001` のログイン時のデフォルトで有効にするロールとして、ユーザーに付与されている `role001` と `role002` を指定します。

```shell
SET DEFAULT ROLE role001, role002 TO user001;
```

## 関連ドキュメント

* [ロールの有効化](../../../../400.guides/500.manage/300.security/300.access-control/200.user-and-permission/340.role-management/500.activate-roles.md)
* [ロールの確認](../../../../400.guides/500.manage/300.security/300.access-control/200.user-and-permission/340.role-management/600.view-roles.md)
* [CURRENT_ROLE](../400.functions/600.information-functions/550.current-role.md)