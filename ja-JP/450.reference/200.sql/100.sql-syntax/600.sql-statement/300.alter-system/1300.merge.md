---

## slug: /merge

# MAJORとMINOR

## 説明

このステートメントは、システムがストレージ層のメジャーコンパクションまたはダンプ（フリーズ）を開始するために使用されます。手動でのメジャーコンパクションとダンプは、インスタンスレベルおよびパーティションレベルをサポートしています。

## 構文

```sql
ALTER SYSTEM merge_action;

merge_action:
      MAJOR FREEZE
    | MAJOR FREEZE  TABLET_ID = tablet_id
    | MINOR FREEZE [TABLET_ID = tablet_id]
    | {SUSPEND | RESUME} MERGE
    | CLEAR MERGE ERROR
```

## パラメータの説明

|          **パラメータ**         |      **説明**      |
|---------------------------|------------------|
| MAJOR FREEZE              | 日次マージを開始します。|
| MINOR FREEZE              | ダンプを開始します。            |
| \{SUSPEND \| RESUME\} MERGE | 日次マージを一時停止または再開します。|
| CLEAR MERGE ERROR         | マージエラーのマーカーをクリアします。|
| MAJOR FREEZE TABLET_ID = tablet_id | TABLET IDを指定してマージ、つまりパーティションレベルでのマージを実行します。|
| TABLET_ID                 | パーティションのマージまたはダンプを指定します。          |
| LS                        | ログストレージのダンプを指定します。          |

## 注意点

パーティションとTabletは一対一で対応しています。パーティション単位のメジャーコンパクションを実行する際には、以下の点に注意してください。

* 対応するパーティションがインスタンス単位のメジャーコンパクションタスクを実行中の場合、パーティション単位のメジャーコンパクションを開始できません。
* 対応するパーティションが適応型スケジューリングのメジャーコンパクションタスクを実行中の場合、パーティション単位のメジャーコンパクションを開始できません。
* メジャーコンパクションタスクが一時停止されている場合、パーティション単位のメジャーコンパクションを開始することはできません。
* パーティション単位のメジャーコンパクションはCPUおよびディスクI/Oを消費します。パーティション単位のメジャーコンパクション操作を実行する前に、現在のユーザーのリソース使用状況を検討する必要があります。また、パーティション単位のメジャーコンパクションコマンドが正常に実行された後は、CPUおよびI/Oの使用率が上昇します。

## 例

### ストレージ層のメジャーコンパクションの例

* システムの日次メジャーコンパクションを開始します。

  ```sql
  ALTER SYSTEM MAJOR FREEZE;
  ```

### ストレージ層のダンプの例

* システムのダンプを開始します。

  ```sql
  ALTER SYSTEM MINOR FREEZE;
  ```

* 指定したパーティションのダンプを実行します。

  ```sql
  ALTER SYSTEM MINOR FREEZE tablet_id = 1100611139453887;
  ```

### 日次メジャーコンパクションの一時停止または再開の例

* 日次メジャーコンパクションを一時停止します。

  ```sql
  ALTER SYSTEM SUSPEND MERGE;
  ```

### メジャーコンパクションエラーマークのクリーンアップの例

* メジャーコンパクションのエラーマークをクリーンアップします。

  ```sql
  ALTER SYSTEM CLEAR MERGE ERROR;
  ```

### パーティションレベルのメジャーコンパクションの開始

1. テーブルのTABLET IDを照会します。

   **例：**

   ```sql
   SELECT t1.tenant_id, t2.tenant_name, t1.database_name, t1.table_id, t1.table_name, t1.tablet_id, t1.PARTITION_NAME, t1.SUBPARTITION_NAME
   FROM oceanbase.CDB_OB_TABLE_LOCATIONS t1, oceanbase.DBA_OB_TENANTS t2
   WHERE t1.tenant_id=t2.tenant_id
   AND t1.table_name = 'test_tbl1'
   AND t2.tenant_name = 'oracle001';
   ```

   実行結果は次のとおりです：

   ```shell
   +-----------+-------------+---------------+----------+------------+-----------+----------------+-------------------+
   | tenant_id | tenant_name | database_name | table_id | table_name | tablet_id | PARTITION_NAME | SUBPARTITION_NAME |
   +-----------+-------------+---------------+----------+------------+-----------+----------------+-------------------+
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200008 | P1             | SP0               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200009 | P1             | SP1               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200010 | P1             | SP2               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200011 | P1             | SP3               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200012 | P2             | SP4               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200013 | P2             | SP5               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200014 | P2             | SP6               |
   |      1004 | oracle001   | SYS           |   500011 | TEST_TBL1  |    200015 | P2             | SP7               |
   +-----------+-------------+---------------+----------+------------+-----------+----------------+-------------------+
   8 rows in set (0.012 sec)
   ```

   詳細については、[oceanbase.CDB_OB_TABLE_LOCATIONS](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002014785)を参照してください。

2. メジャーコンパクションを開始します。

   **例：**

   ```sql
   ALTER SYSTEM MAJOR FREEZE TENANT = oracle001 TABLET_ID = 200008;
   ```

### パーティションレベルのダンプを開始する

1. テーブルのTABLET IDを照会します。

   **例：**

   ```sql
   SELECT database_name, table_id, table_name, tablet_id, PARTITION_NAME, SUBPARTITION_NAME
   FROM oceanbase.CDB_OB_TABLE_LOCATIONS
   WHERE table_name = 'tbl1';
   ```

   実行結果は次のとおりです：

    ```shell
    +---------------+----------+------------+-----------+----------------+-------------------+
    | database_name | table_id | table_name | tablet_id | PARTITION_NAME | SUBPARTITION_NAME |
    +---------------+----------+------------+-----------+----------------+-------------------+
    | test_db       |   500197 | tbl1       |    200067 | NULL           | NULL              |
    | test_table    |   500557 | tbl1       |    200276 | NULL           | NULL              |
    | test_data     |   500658 | tbl1       |    200337 | NULL           | NULL              |
    | mysql         |   500722 | tbl1       |    200365 | NULL           | NULL              |
    +---------------+----------+------------+-----------+----------------+-------------------+
    4 rows in set (0.277 sec)
    ```

   詳細については、[oceanbase.CDB_OB_TABLE_LOCATIONS](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002014785)を参照してください。

2. ダンプを開始します。

   **例：**

   ```sql
   ALTER SYSTEM MINOR FREEZE TABLET_ID = 200067;
   ```

## 関連ドキュメント

* [メジャーコンパクションの管理について](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013689)
* [メジャーコンパクションの手動トリガー](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013687)
* [メジャーコンパクション情報の確認](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013690)
* [ダンプの管理について](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013694)
* [ダンプの手動トリガー](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013696)
* [ダンプ情報の確認](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002013693)