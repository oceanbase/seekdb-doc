---

## slug: /json-formatted-data-type-conversion-of-syntax

# JSONデータ型の変換

seekdbは、CAST関数を使用して、他のデータ型とJSONデータ型との相互変換をサポートします。

JSONデータ型の変換ルールは以下の表のとおりです。

|               その他のデータ型                |          CAST(other_type AS JSON)           |                 CAST(JSON AS other_type)                 |
|-------------------------------------|---------------------------------------------|----------------------------------------------------------|
| JSON                                | 変更なし                                        | 変更なし                                                     |
| utf8文字型 (utf8mb4、utf8、asciiを含む) | 文字がJSON型値に変換され、有効性が検証されます。                     | utf8mb4文字列にシリアライズされます。                                        |
| その他の文字セット型                             | まずutf8mb4文字エンコードに変換し、次にutf8文字型の説明に従って変換します。 | まずutf8mb4エンコードの文字列にシリアライズし、次に対応する文字セットの文字列に変換します。                   |
| NULL                                | 空のJSON型値を返します。                               | 該当なし                                                      |
| その他の型                                | 単一のスカラー値のみが、個別の値を持つJSON型値に変換されます。            | JSON型値が単一のスカラー値のみを含み、かつ対象型と一致する場合は、対応する型に変換されます。そうでない場合は、NULLを返し、警告を発します。 |

  :::info

  `other_type` は、JSON以外の他のデータ型を指します。

  :::

以下にいくつかの変換例を示します。

```sql
SELECT CAST("123" AS JSON);
```

戻り値は次のとおりです：

```sql
+---------------------+
| CAST("123" AS JSON) |
+---------------------+
| 123                 |
+---------------------+
1 row in set (0.001 sec)
```

```sql
SELECT CAST(null AS JSON);
```

戻り値は次のとおりです：

```sql
+--------------------+
| CAST(null AS JSON) |
+--------------------+
| NULL               |
+--------------------+
1 row in set (0.001 sec)
```

```sql
CREATE TABLE tj1 (c1 JSON,c2 VARCHAR(20));

INSERT INTO tj1 VALUES ('{"id": 17, "color": "red"}','apple'),('{"id": 18, "color": "yellow"}', 'banana'),('{"id": 16, "color": "orange"}','orange');

SELECT * FROM tj1 ORDER BY CAST(JSON_EXTRACT(c1, '$.id') AS UNSIGNED);
```

戻り値は次のとおりです：

```sql
+-------------------------------+--------+
| c1                            | c2     |
+-------------------------------+--------+
| {"id": 16, "color": "orange"} | orange |
| {"id": 17, "color": "red"}    | apple  |
| {"id": 18, "color": "yellow"} | banana |
+-------------------------------+--------+
3 rows in set (0.003 sec)
```