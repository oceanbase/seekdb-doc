---
slug: /default-embedding-function-of-api
---

# デフォルトの埋め込み関数

埋め込み関数は、テキスト文書をベクトル埋め込みに変換し、類似性検索を実行します。pyseekdbは、組み込みの埋め込み関数とカスタム埋め込み関数をサポートしています。

指定がない場合、`DefaultEmbeddingFunction`がデフォルトの埋め込み関数となります。この関数はseekdbに既に存在しており、新たに作成する必要はありません。

例：

```python
from pyseekdb import DefaultEmbeddingFunction

# Use default model (all-MiniLM-L6-v2, 384 dimensions)
ef = DefaultEmbeddingFunction()

# Use custom model
ef = DefaultEmbeddingFunction()

# Get embedding dimension
print(f"Dimension: {ef.dimension}")  # 384

# Generate embeddings
embeddings = ef(["Hello world", "How are you?"])
print(f"Generated {len(embeddings)} embeddings, each with {len(embeddings[0])} dimensions")
```

## 関連操作

カスタム関数を使用する必要がある場合は、以下のドキュメントを参照してカスタム関数の作成と使用を行ってください：

* [カスタム埋め込み関数の作成](200.create-custim-embedding-functions-of-api.md)
* [カスタム埋め込み関数の使用](300.using-custom-embedding-functions-of-api.md)