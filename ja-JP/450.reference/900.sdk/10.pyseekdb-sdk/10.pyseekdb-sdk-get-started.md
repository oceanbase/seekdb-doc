---

## slug: /pyseekdb-sdk-get-started

# クイックスタート

## pyseekdb

pyseekdbは、OceanBaseが提供するPythonクライアントです。一連のAPIを基盤として実装されており、2種類のデータベース接続モードを提供し、組み込みモードのseekdbへの接続や、リモートサーバーへの接続(OceanBaseデータベースへの接続)をサポートしています。

:::tip
OceanBaseデータベースは、OceanBaseが提供する完全に独自開発されたエンタープライズ向けネイティブ分散型データベースです。一般的なハードウェア上で金融級の高可用性を実現し、「三地五中心」という都市レベルの障害に対する自動無損失ディザスタリカバリという新標準を確立しました。TPC-Cベンチマークテストで新記録を樹立し、単一クラスタで1,500ノードを超える規模をサポートします。クラウドネイティブ、強整合性、Oracle/MySQLとの高い互換性などの特徴を備えています。OceanBaseデータベースの詳細については、[OceanBaseデータベース](https://jp.oceanbase.com/docs/oceanbase-database)を参照してください。
:::

pyseekdbは現在、Linux、macOS、Windowsシステムでの使用をサポートしています。異なるオペレーティングシステムでサポートされているデータベース接続モードは異なる場合があります。詳細は以下の表を参照してください。

| システム | 埋め込み型seekdb | サーバー型seekdb | サーバー型OceanBaseデータベース |
| ---- | ------- | ------- | ------- |
| Linux | サポート | サポート | サポート |
| macOS | 現在はサポートされていません | サポート | サポート |
| Windows | 現在はサポートされていません | サポート | サポート |

Linuxシステムにこのクライアントをインストールすると、組み込みモードのseekdbも同時にインストールされ、組み込みseekdbに直接接続してデータベースの作成などの操作を実行できます。また、既にデプロイ済みのサーバーモードのseekdbやOceanBaseデータベースにリモート接続することも選択できます。

## pyseekdbのインストール

### 前提条件

環境が以下の要件を満たしていることを確認してください：

* オペレーティングシステム：Linux (glibc >= 2.28)、macOS、Windows
* Pythonバージョン：Python 3.11以降のバージョン
* システムアーキテクチャ：x86_64、aarch64

### インストール

pipを使用してインストールすると、デフォルトのPythonバージョンとプラットフォームが自動的に認識されます。

```shell
pip install -U pyseekdb
```

pipのバージョンが古い場合は、インストール前にpipをアップグレードしてください。

```bash
pip install --upgrade pip
```

## 次のステップ

* pyseekdbのインストール後、seekdbに接続して操作を実行できます。pyseekdbがサポートするAPIについては、[APIリファレンス](50.apis/10.api-overview.md)を参照してください。

* 当社が提供するSDKサンプルを参考に、pyseekdbをすぐに試すこともできます。

  * [Simpleサンプル](30.sdk-samples/10.pyseekdb-simple-sample.md)

  * [Completeサンプル](30.sdk-samples/50.pyseekdb-complete-sample.md)

  * [ハイブリッド検索サンプル](30.sdk-samples/100.pyseekdb-hybrid-search-sample.md)