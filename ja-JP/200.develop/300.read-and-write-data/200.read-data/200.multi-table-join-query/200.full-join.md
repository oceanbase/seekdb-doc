---

## slug: /full-join

# FULL JOIN

完全結合（FULL JOIN）とは、完全外部結合を表します。このステートメントを使用することで、結合したすべてのテーブルのすべての行を、マッチするかどうかに関係なく返すことができます。

## 背景

外部結合（OUTER JOIN）は、比較演算子を使用して2つのテーブルのデータを比較します。結合結果には結合条件に合致する行だけでなく、条件に合致しない行も含まれます。

外部結合には、完全結合（FULL JOIN）、左結合（LEFT JOIN）、右結合（RIGHT JOIN）が含まれます。外部結合は結合条件を満たすすべての行を返し、一方のテーブルから未使用の行を返すときは、もう一方のテーブルの対応する位置に`NULL`を入力します。

本記事では主に完全結合の例を説明します。左結合に関連する例については、[左結合](300.left-join.md) を、右結合に関連する例については [右結合](400.right-join.md) を参照してください。

## FULL JOIN構文

外部結合ステートメントには通常、左テーブルと右テーブルが含まれます。`JOIN` 句の一番左側に記述されているテーブルが左テーブル、`JOIN` 句の一番右側に記述されているテーブルが右テーブルです。

FULL JOINの構文は以下のとおりです：

```sql
SELECT select_list FROM table_name1 FULL JOIN table_name2 ON join_condition
[ WHERE query_condition ]
[ ORDER BY column_list ];
```

ここで、`table_name1` が左テーブル、`table_name2` が右テーブルです。

## 例

サンプルテーブルを作成し、適切なデータを挿入します。

```sql
CREATE TABLE tbl_a(id INT NOT NULL PRIMARY KEY, name VARCHAR(50));
Query OK, 0 rows affected

CREATE TABLE tbl_b(number INT NOT NULL PRIMARY KEY, value INT);
Query OK, 0 rows affected

INSERT INTO tbl_a VALUES(1,'ab'),(2,'cd'),(3,'ef'),(4,'gh');
Query OK, 4 rows affected
Records: 4  Duplicates: 0  Warnings: 0

INSERT INTO tbl_b VALUES(1,1001),(3,1003),(5,1005);
Query OK, 4 rows affected
Records: 4  Duplicates: 0  Warnings: 0
```

### シンプルな完全結合クエリ

完全結合クエリを使用する場合、左テーブルまたは右テーブルの行でマッチする行が見つからない場合、システムは自動的に `NULL` を埋めます。

例：テーブル `tbl_a` とテーブル `tbl_b` に対して完全結合クエリを実行し、実行結果を取得します。

```sql
SELECT * FROM tbl_a;
+------+------+
| id   | name |
+------+------+
|    1 | ab   |
|    2 | cd   |
|    3 | ef   |
|    4 | gh   |
+------+------+
4 rows in set

SELECT * FROM tbl_b;
+--------+-------+
| number | value |
+--------+-------+
|      1 |  1001 |
|      3 |  1003 |
|      5 |  1005 |
+--------+-------+
3 rows in set

SELECT * FROM tbl_a FULL JOIN tbl_b ON tbl_a.id=tbl_b.number;
+------+------+--------+-------+
| id   | name | number | value |
+------+------+--------+-------+
|    1 | ab   |      1 |  1001 |
|    2 | cd   |   NULL |  NULL |
|    3 | ef   |      3 |  1003 |
|    4 | gh   |   NULL |  NULL |
| NULL | NULL |      5 |  1005 |
+------+------+--------+-------+
5 rows in set
```

ご覧のように、テーブル `tbl_a` には値が `5` の行がないため、実行結果ではその行のレコードが `NULL` に埋め込まれています。同様に、テーブル `tbl_b` には値が `2` および `4` の行がないため、実行結果ではこれら2行が `NULL` に埋め込まれています。

### 完全結合とWHERE句の組み合わせ使用

完全結合で結合結果を取得した後、`WHERE` 句を使用して結合結果をフィルタリングできます。

例2：テーブル `tbl_a` とテーブル `tbl_b` に対して完全結合クエリを実行した後、`tbl_b` テーブルの `value=1003` のデータを返します。

```sql
SELECT * FROM tbl_a;
+------+------+
| id   | name |
+------+------+
|    1 | ab   |
|    2 | cd   |
|    3 | ef   |
|    4 | gh   |
+------+------+
4 rows in set

SELECT * FROM tbl_b;
+--------+-------+
| number | value |
+--------+-------+
|      1 |  1001 |
|      3 |  1003 |
|      5 |  1005 |
+--------+-------+
3 rows in set

SELECT * FROM tbl_a FULL JOIN tbl_b ON tbl_a.id=tbl_b.number WHERE tbl_b.value=1003;
+------+------+--------+-------+
| id   | name | number | value |
+------+------+--------+-------+
|    3 | ef   |      3 |  1003 |
+------+------+--------+-------+
1 row in set
```

## 関連ドキュメント

* [INNER JOIN](100.inner-join.md)

* [LEFT JOIN](300.left-join.md)

* [RIGHT JOIN](400.right-join.md)