---
slug: /use-time-functions-in-queries
---

# クエリでの時間関数の使用

本記事では、具体的な例を通じてクエリ内で時間関数を使用する方法を紹介します。

## 時間関数

seekdbでよく使われる時間型には、DATE、TIMESTAMP、TIME、DATETIME、YEARなどがあります。seekdbでは、よく使われる時間関数には、NOW()、CURDATE()、CURTIME()などがあります。

## 例

### 時間型の表示形式を調整する

seekdbでは、`NOW()` を使用して現在時刻を出力し、`DATE_FORMAT` 関数を使用して時間型の表示形式を調整できます。

```sql
SELECT NOW(),DATE_FORMAT(NOW(),"%Y/%M/%D %T") new_time;
+---------------------+-----------------------------+
| NOW()               | new_time                    |
+---------------------+-----------------------------+
| 2025-12-23 18:15:36 | 2025/December/23rd 18:15:36 |
+---------------------+-----------------------------+
1 row in set
```

### 時間から年、月、日、時、分、秒を抽出する

seekdbでは、`EXTRACT()` 関数を使用して、時間から年、月、日、時、分、秒の情報を抽出できます。

```sql
SET @dt = NOW();
Query OK, 0 rows affected

SELECT @dt
    , EXTRACT(YEAR FROM @dt)    d_year
    , EXTRACT(MONTH FROM @dt)   d_month
    , EXTRACT(week FROM @dt)    d_week
    , EXTRACT(DAY FROM @dt)     d_day
    , EXTRACT(HOUR FROM @dt)    d_hour
    , EXTRACT(MINUTE FROM @dt)  d_min
    , EXTRACT(SECOND FROM @dt)  d_second
    , EXTRACT(year_month FROM @dt)  d_year_month
    , EXTRACT(hour_minute FROM @dt) d_hour_min
\G

*************************** 1. row ***************************
         @dt: 2025-12-23 18:15:53
      d_year: 2025
     d_month: 12
      d_week: 51
       d_day: 23
      d_hour: 18
       d_min: 15
    d_second: 53
d_year_month: 202512
  d_hour_min: 1815
1 row in set
```

### 時間型の加算と減算

seekdbでは、`DATE_ADD()` または `DATE_SUB()` 関数を使用して時間の加算と減算を行えます。SQLステートメントの例は以下のとおりです：

```sql
SET @dt = NOW();
Query OK, 0 rows affected

SELECT @dt
    , DATE_ADD(@dt, INTERVAL 1 DAY )        t1
    , DATE_ADD(@dt, INTERVAL 1 HOUR )       t2
    , DATE_ADD(@dt, INTERVAL -10 MINUTE )   t3
    , DATE_ADD(@dt, INTERVAL -1 MONTH )     t4
    , DATE_ADD(@dt, INTERVAL 1 YEAR )       t5
\G

*************************** 1. row ***************************
@dt: 2025-12-23 18:16:11
 t1: 2025-12-24 18:16:11
 t2: 2025-12-23 19:16:11
 t3: 2025-12-23 18:06:11
 t4: 2025-11-23 18:16:11
 t5: 2026-12-23 18:16:11
1 row in set
```

その他の時間型の使用方法については、**「SQLリファレンス」** の章を参照してください。