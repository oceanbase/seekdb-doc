---

## slug: /turn-the-recyclebin-on-or-off

# ごみ箱の有効化または無効化

seekdbでは、システム変数 `recyclebin` を使用してごみ箱の有効化と無効化を制御します。デフォルトではごみ箱は無効な状態です。ごみ箱機能を有効にすると、データベースオブジェクトに対してDrop操作を実行すると、そのオブジェクトはごみ箱に移動します。

## 背景

seekdbのシステム変数は、グローバル変数とセッション変数に分けられます。ゴミ箱を有効または無効にする際には、必要に応じて適切な設定方法を選択できます。

* グローバル変数：Globalレベルの変更を表します。データベース内の異なるユーザー間でグローバル変数は共有されます。グローバル変数の変更は、セッションの終了によって無効になることはありません。また、グローバル変数を変更しても、その変更は現在開いているセッションには反映されません。変更を有効にするには、新しいセッションを確立する必要があります。

* セッション変数：Sessionレベルの変更を表します。クライアントがデータベースに接続すると、データベースはグローバル変数をコピーして自動的にセッション変数を生成します。セッション変数の変更は、現在のセッション内でのみ有効です。

## 手順

1. 管理者ユーザー`root`でseekdbにログインします。

   接続例は以下の通りです。データベースへの接続時は、実際の環境に合わせてください。

   ```shell
   mysql -h127.0.0.1 -uroot -P2881
   ```

2. 以下のステートメントを実行して、ゴミ箱の状態を確認します。

   ```shell
   SHOW VARIABLES LIKE 'recyclebin';
   ```

   ```shell
   +---------------+-------+
   | Variable_name | Value |
   +---------------+-------+
   | recyclebin    | ON    |
   +---------------+-------+
   1 row in set
   ```

3. ビジネスニーズに基づき、適切な方法でゴミ箱機能を有効または無効にします。

   変数`recyclebin`はBool型の変数であるため、複数の値をサポートしています。具体的には：

   * `on`、`1`、または `true`：ゴミ箱を有効にすることを表します。

   * `off`、`0`、または `false`：ゴミ箱を無効にすることを表します。

   * グローバル（Global）レベルでのゴミ箱の有効化または無効化

     Global設定は、seekdb全体で有効になります。設定後は、現在のセッションには無効であり、変更を有効にするには、再ログインして新しいセッションを確立する必要があります。

     ステートメントは以下の通りです：

     ```shell
     SET GLOBAL recyclebin = on;
     ```

     ```shell
     SET GLOBAL recyclebin = off;
     ```

     または

     ```shell
     SET GLOBAL recyclebin = 1;
     ```

     ```shell
     SET GLOBAL recyclebin = 0;
     ```

     または

     ```shell
     SET GLOBAL recyclebin = true;
     ```

     ```shell
     SET GLOBAL recyclebin = false;
     ```

   * 現在のセッション（Session）レベルでのゴミ箱の有効化または無効化

     Session設定は、現在のセッション内でのみ有効です。

     ```shell
     SET @@recyclebin = on;
     ```

     ```shell
     SET @@recyclebin = off;
     ```

     または

     ```shell
     SET @@recyclebin = 1;
     ```

     ```shell
     SET @@recyclebin = 0;
     ```

     または

     ```shell
     SET @@recyclebin = true;
     ```

     ```shell
     SET @@recyclebin = false;
     ```

## 関連ドキュメント

ゴミ箱に関する詳細情報については、以下を参照してください：

* [ゴミ箱の概要](../200.recyclebin/100.recyclebin-overview.md)

* [ゴミ箱オブジェクトの確認](../200.recyclebin/300.view-the-recyclebin-objects.md)

* [ゴミ箱のクリーンアップ](../200.recyclebin/500.purge-the-recyclebin.md)

* [ゴミ箱オブジェクトの復元](../200.recyclebin/400.restore-the-recyclebin-objects.md)