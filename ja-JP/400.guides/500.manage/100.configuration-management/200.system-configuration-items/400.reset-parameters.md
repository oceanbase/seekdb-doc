---
slug: /reset-parameters
---

# 構成パラメータのリセット

構成パラメータの値を変更した後、ビジネスニーズに応じて `ALTER SYSTEM RESET` ステートメントを使用して、その構成パラメータの値をデフォルト値にリセットできます。

## 手順

1. seekdbにログインします。

   接続例は以下のとおりですが、データベースへの接続時は実際の環境に準じてください。

   ```shell
   mysql -h127.xx.xx.xx -P2881 -uroot -p***** -A
   ```

2. 以下のコマンドを実行して、パラメータの値を確認します。

   ```sql
   SHOW PARAMETERS LIKE '%enable_sql_audit%';
   ```

   クエリ結果は次のとおりです：

   ```shell
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   | name             | data_type | value | info                                                                                                          | edit_level        | default_value | isdefault |
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   | enable_sql_audit | BOOL      | False | specifies whether SQL audit is turned on. The default value is TRUE. Value: TRUE: turned on FALSE: turned off | DYNAMIC_EFFECTIVE | true          |         0 |
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   1 row in set
   ```

3. 以下のコマンドを実行して、パラメータの値をリセットします。

   ステートメントは以下のとおりです：

   ```shell
   ALTER SYSTEM RESET
       parameter_name [SCOPE = {SPFILE | BOTH}]
   ```

   関連パラメータの説明は以下のとおりです：

   * `SCOPE`は、パラメータの有効範囲を指定するために使用されます。デフォルト値は`BOTH`です。詳細は以下のとおりです：

     * `SPFILE`：設定テーブル内のパラメータ値のみをリセットします。seekdbが再起動した後に有効になります。

     * `BOTH`：設定テーブルとメモリ内の値の両方をリセットします。変更は即時に反映され、seekdbが再起動した後も設定値は有効です。

    例：

    * パラメータの値をリセットします。

      ```sql
      ALTER SYSTEM RESET enable_sql_audit;
      ```

4. リセットが成功した後、実際の状況（seekdbを再起動するかどうか）に応じて、`SHOW PARAMETERS`ステートメントを実行して、パラメータの値が正常にリセットされたかどうかを確認できます。

   ```sql
   SHOW PARAMETERS LIKE '%enable_sql_audit%';
   ```

   結果は次のとおりです：

   ```shell
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   | name             | data_type | value | info                                                                                                          | edit_level        | default_value | isdefault |
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   | enable_sql_audit | BOOL      | True  | specifies whether SQL audit is turned on. The default value is TRUE. Value: TRUE: turned on FALSE: turned off | DYNAMIC_EFFECTIVE | true          |         1 |
   +------------------+-----------+-------+---------------------------------------------------------------------------------------------------------------+-------------------+---------------+-----------+
   1 row in set
   ```

## 関連ドキュメント

* [パラメータの概要](100.configuration-items-overview.md)

* [パラメータの表示](200.view-parameters.md)

* [パラメータの変更](300.modify-parameters.md)

* [システム変数の概要](../300.system-variable/100.system-variable-overview.md)