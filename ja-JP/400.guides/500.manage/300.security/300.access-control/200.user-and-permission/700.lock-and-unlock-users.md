---

## slug: /lock-and-unlock-users

# ユーザーのロックとアンロック

ユーザーが一時的にデータベースへのアクセスを必要としない場合、管理者はそのユーザーをロックできます。ロックされたユーザーはseekdbにログインできません。また、管理者はユーザーのロックを解除し、ロックされたユーザーを再び有効化することもできます。

## 前提条件

ユーザーのロックとアンロック操作は通常、管理者が実行します。一般ユーザーがロックおよびアンロック操作を実行する必要がある場合は、グローバルな`ALTER USER`権限が付与されていなければなりません。現在持っている権限を確認するには、[ユーザー権限の確認](../200.user-and-permission/400.view-user-permissions.md)を参照してください。グローバルな`ALTER USER`権限がない場合は、管理者に連絡し、追加してもらってください。ユーザー権限の付与に関する操作については、[直接権限付与](200.direct-authority.md)を参照してください。

## 手順

1. seekdbにログインします。

2. 以下のステートメントを実行して、ユーザーをロックまたはアンロックします。

   ステートメントは以下のとおりです：

   ```sql
   ALTER USER user_name ACCOUNT LOCK | UNLOCK;
   ```

   例：

   * ユーザーのロック

     ```shell
     ALTER USER demo ACCOUNT LOCK;
     ```

     ```shell
     mysql -h127.0.0.1 -uroot -P2881 -p******
     ```

     ```shell
     ERROR 3118 (HY000): User locked
     ```

   * ユーザーのアンロック

     ```shell
     ALTER USER demo ACCOUNT UNLOCK;
     ```

     ```shell
     Query OK, 0 rows affected (0.02 sec)
     ```

     ```shell
     mysql -h127.0.0.1 -uroot -P2881  -p******
     ```

      ```shell
      Welcome to the MariaDB monitor.  Commands end with ; or \g.
      Your MySQL connection id is 3221537922
      Server version: 5.7.25 OceanBase 4.3.5.3 SeekDB (r1.0.0.0) (Built 100000262025111218-5343637512e28c346f938516af53b7879d4d5974 Nov 12 2025)

      Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

      Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

      MySQL [(none)]>
      ```

3. ユーザーのロックおよびアンロック操作を実行した後、管理者は`DBA_OB_USERS`ビューの`is_locked`フィールドをクエリすることで、ユーザーのロック状態を確認できます。

   例：

   ```shell
   SELECT user_name,is_locked FROM oceanbase.DBA_OB_USERS WHERE user_name='demo';
   ```

   ```shell
   +-----------+-----------+
   | user_name | is_locked |
   +-----------+-----------+
   | demo      | NO        |
   +-----------+-----------+
   1 row in set
   ```

   `is_locked`フィールドの値が`YES`の場合、ユーザーはロック状態です。`is_locked`フィールドの値が`NO`の場合、ユーザーはアンロック状態です。

## 関連ドキュメント

`ALTER USER`ステートメントの詳細については、[ALTER USER](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002017003)を参照してください。