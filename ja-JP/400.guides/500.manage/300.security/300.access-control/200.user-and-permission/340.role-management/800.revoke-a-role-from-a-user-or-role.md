---
slug: /revoke-a-role-from-a-user-or-role
---

# ユーザーまたはロールからロールを取り消す

`REVOKE` ステートメントを使用して、ユーザーまたは他のロールからロールを取り消すことができます。

現在、1つのロールを複数のユーザーまたはロールから取り消すこと、1人のユーザーまたは1つのロールから複数のロールを取り消すこと、および複数のユーザーまたはロールから複数のロールを取り消すことがサポートされています。

## 前提条件

ロールを取り消す際、現在のユーザーは取り消す対象のロールを所有している必要があり、かつ `ADMIN OPTION` 権限を持っていなければなりません。現在のユーザーまたはロールが所有するロールの詳細な操作については、[ロールの確認](600.view-roles.md)を参照してください。

## 注意事項

`REVOKE` ステートメントを実行してユーザーからロールを取り消す場合、その変更は現在ログインしているユーザーには即時には反映されません。

## 手順例

### ユーザーからロールを取り消す

* ユーザー `test1` から `employee` ロールを取り消す

  ```sql
  REVOKE employee FROM test1;
  ```

* ユーザー `test1` および `test2` から `employee` ロールを取り消す

  ```sql
  REVOKE employee FROM test1,test2 IGNORE UNKNOWN USER;
  ```

  この例では、`IGNORE UNKNOWN USER` 句は、取り消すロールを所有するユーザーが存在するかどうかを確認するために使用されます。`IGNORE UNKNOWN USER` 句を追加しない場合、`test1` または `test2` のいずれか一方のユーザーが存在しない場合、システムはエラーを返します。`IGNORE UNKNOWN USER` 句を追加すると、システムは警告メッセージを表示します。

  `IGNORE UNKNOWN USER` 句を追加するかどうかにかかわらず、`test1` または `test2` のいずれか一方のユーザーが存在しない場合、ロールの取り消し操作は失敗します。

* ユーザー `test1` から `employee` および `developer` ロールを取り消す

  ```sql
  REVOKE IF EXISTS employee,developer FROM test1 IGNORE UNKNOWN USER;
  ```

  この例では、`IF EXISTS` 句は、取り消すロールが存在するかどうかを確認するために使用されます。`IF EXISTS` 句を追加しない場合、`employee` または `developer` ロールのいずれか一方が存在しない場合、システムはエラーを返します。`IF NOT EXISTS` 句を追加すると、システムは警告メッセージを表示します。

  `IF EXISTS` 句を追加するかどうかにかかわらず、`employee` または `developer` ロールのいずれか一方が存在しない場合、ロールの取り消し操作は失敗します。

### ロールからロールを取り消す

* `role1` ロールから `employee` ロールを取り消す

  ```sql
  REVOKE employee FROM role1;
  ```

* `role1` および `role2` ロールから `employee` ロールを取り消す

  ```sql
  REVOKE IF EXISTS employee FROM role1,role2;
  ```

## 関連ドキュメント

* [権限の取り消し](../340.role-management/700.revoke-privileges-from-a-role.md)

* `REVOKE` ステートメントの詳細については、[REVOKE](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002017006)を参照してください。